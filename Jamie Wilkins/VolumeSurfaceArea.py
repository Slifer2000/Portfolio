import math
Length = input("What is the length of the cylinder? ")
Diameter = input("What is the diameter of the cylinder? ")
Radius = (float(Diameter/2)
Volume = (math.pi*(float(Radius))*(float(Radius))*(float(Length)))
